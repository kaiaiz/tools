# GitLab 配置示例文件
# 复制此文件为 .env 并填写你的实际配置值

# GitLab 实例的基础 URL（不包含具体的仓库路径）
# 例如：http://git.tyjfwy.com 或 https://gitlab.com
# 注意：不要包含仓库路径，如 /root/finance_backend.git
GITLAB_ROOT_URL=http://git.tyjfwy.com

# GitLab Personal Access Token
# 获取方法：
# 1. 登录 GitLab → 点击头像 → Settings → Access Tokens
# 2. 创建新 Token，勾选 read_api 和 read_repository 权限
# 3. 复制生成的 Token 粘贴到下面
GITLAB_TOKEN=your_token_here

# 统计的开始日期（格式：YYYY-MM-DD）
START_DAY=2022-01-01

# 统计的结束日期（格式：YYYY-MM-DD）
END_DAY=2025-01-01

# 指定要统计的仓库列表（可选，留空则统计所有仓库）
# 多个仓库用逗号分隔，支持仓库名称或完整路径（path_with_namespace）
# 例如：GITLAB_PROJECTS=finance_backend,backend-api,root/frontend
# 如果为空或未设置，则统计所有仓库
GITLAB_PROJECTS=

# 指定要统计的分支列表（可选，留空则使用各仓库的默认分支）
# 多个分支用逗号分隔
# 例如：GITLAB_BRANCHES=dev,main
# 如果为空或未设置，则使用各仓库的默认分支
GITLAB_BRANCHES=

# 排除配置（可选）
# 以下配置用于过滤不需要统计的仓库

# 排除指定路径的仓库（多个路径用逗号分隔）
# 例如：EXCLUDE_PATHS=root/test,root/demo
EXCLUDE_PATHS=

# 排除以指定前缀开头的仓库名（多个前缀用逗号分隔）
# 例如：EXCLUDE_PREFIX=test-,demo-,temp-
EXCLUDE_PREFIX=

# 排除指定名称的项目（多个项目名用逗号分隔，完全匹配）
# 例如：EXCLUDE_PROJECT=仓库1,仓库2,仓库3
EXCLUDE_PROJECT=
